package blub.prolog.clauses;

import blub.prolog.Clause;

/**
 * A relation is a fact-clause that has atoms as one or more of the first 
 * arguments.
 * 
 * It defines a "relation" between those atoms. 
 */
class RelationClause extends FactClause {
    
}
